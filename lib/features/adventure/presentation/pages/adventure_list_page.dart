import 'package:flutter/material.dart';

class AdventureListPage extends StatelessWidget {
  const AdventureListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Adventure & Nature')),
      body: const Center(child: Text('Adventure List - Coming Soon')),
    );
  }
}
